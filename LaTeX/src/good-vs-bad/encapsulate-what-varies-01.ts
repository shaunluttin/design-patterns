if (pet.type() == dog) {
    pet.bark();
} else if (pet.type() == cat) {
    pet.meow();
} else if (pet.type() == duck) {
    pet.quack();
}

// The number of animals that our app supports might change.

pet.speak();
